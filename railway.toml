[build]
buildCommand = "pip install -r requirements.txt && python manage.py collectstatic --noinput"

[deploy]
startCommand = "python manage.py migrate && .venv/bin/gunicorn washy.wsgi:application --bind 0.0.0.0:$PORT"